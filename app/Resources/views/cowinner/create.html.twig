{% extends 'base.html.twig' %}

{% block body %}
    
    <div class="content">

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <a href="{{ path('home') }}" class="btn btn-info">bring me back home...</a>

    </div>

{% endblock %}
